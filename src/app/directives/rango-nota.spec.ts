import { RangoNota } from './rango-nota';

describe('RangoNota', () => {
  it('should create an instance', () => {
    const directive = new RangoNota();
    expect(directive).toBeTruthy();
  });
});
